# Market - Solana ZK Identity Protocol

Market 是一个基于 Solana 区块链的去中心化身份（Identity）与信用评分（Credit Score）协议。该项目结合了 Anchor 智能合约与 React 前端，利用零知识证明（ZK Proofs）和科学的评分算法来管理链上身份与信誉。

## ✨ 主要功能 (Features)

- **身份管理 (Identity Management)**: 用户可以在链上注册、验证、取消验证和删除自己的身份账户。
- **信用评分 (Credit Scoring)**: 基于链上 SOL 持有量计算用户的信用分数，要求身份必须经过验证。
- **分数管理 (Score Management)**: 用户可以计算、更新和删除自己的信用分数。
- **事件日志 (Event Logging)**: 合约中添加了完整的事件日志，便于前端和监控系统追踪状态变化。
- **模块化设计**: 评分逻辑和身份验证逻辑分离，便于未来扩展和维护。
- **全栈交互**: 提供完整的 React 前端界面，通过生成的 SDK 与链上程序无缝交互。

## 💡 核心概念 (Core Concepts)

### 1. 为什么需要身份证明 (Identity)?

在区块链上，默认情况下地址是匿名的，系统无法识别背后的操作者。

- **防范女巫攻击 (Sybil Resistance)**: 防止恶意用户生成大量地址进行欺诈。
- **状态锚定**: `IdentityAccount` 为钱包地址提供了一个链上“身份证”，记录创建时间与验证状态，使业务逻辑（如信用分）可以绑定在固定身份上。
- **可管理性**: 用户可以随时取消验证或删除自己的身份，保持对个人数据的完全控制。

### 2. 为什么需要身份验证 (Identity Verification)?

身份验证是信誉系统的基础，确保用户身份的真实性和唯一性：

- **增强信任**: 验证过的身份更能被其他用户和应用信任。
- **启用高级功能**: 只有验证过的身份才能计算和使用信用分数。
- **可撤销机制**: 用户可以随时撤销自己的身份验证，保持对个人数据的控制。

### 3. 为什么需要信誉分 (Credit Score)?

DeFi 通常需要超额抵押，缺乏传统金融的信用评估体系。引入信誉分可以：

- **降低抵押率**: 高分用户可享受更优的借贷条件。
- **差异化服务**: DApp 可根据分数提供分级服务。
- **抗女巫攻击**: 结合身份验证，防止恶意用户通过多个账户获取高分数。
- **可管理性**: 用户可以随时删除自己的信用分数，保持对个人数据的完全控制。

#### 当前评分逻辑 (Scoring Logic)

目前的评分模型基于用户的 **SOL 持有量 (Lamports)** 进行计算，**仅对已验证身份可用**：

| SOL 余额   | 分数 (Score) | 等级 (Level) |
| :--------- | :----------- | :----------- |
| ≥ 10 SOL   | 90           | High         |
| ≥ 1 SOL    | 75           | Medium       |
| ≥ 0.1 SOL  | 60           | Low          |
| ≥ 0.01 SOL | 45           | Low          |
| 其他       | 30           | Low          |

> _注：当前逻辑位于合约的 `src/instructions/score.rs`，未来将扩展更多维度（如交易历史、持有时长等）。评分逻辑已模块化，便于未来升级。_

## 🛠 技术栈 (Tech Stack)

- **Smart Contract**: [Solana](https://solana.com/), [Anchor Framework](https://www.anchor-lang.com/), Rust
- **Frontend**: [React](https://react.dev/), [Vite](https://vitejs.dev/), [Tailwind CSS](https://tailwindcss.com/)
- **SDK Generation**: [Codama](https://github.com/codama-idl/codama)
- **State Management**: Zustand

## 📋 环境要求 (Prerequisites)

在开始之前，请确保您的开发环境已安装以下工具：

- **Node.js** (建议 v18+)
- **Rust & Cargo** (最新稳定版)
- **Solana CLI** (v1.16+)
- **Anchor AVM** (最新版)

## 🚀 快速开始 (Quick Start)

### 合约部署

```bash
./deploy_local.sh
```

### 启动前端

```bash
npm run dev
```

打开浏览器访问 `http://localhost:5173` 即可看到应用界面。


## 📜 常用脚本 (Scripts)

- `npm run dev`: 启动前端开发服务器。
- `npm run build`: 构建前端生产版本（TypeScript 编译 + Vite 构建）。
- `npm run anchor-build`: 仅构建 Solana 合约。
- `npm run anchor-test`: 运行合约测试（跳过部署）。
- `npm run codama:js`: 仅根据 IDL 生成前端 SDK。
- `npm run setup`: 执行完整的构建和生成流程（合约构建 + SDK 生成）。
- `npm run lint`: 运行 ESLint 检查代码质量。
- `npm run format`: 使用 Prettier 格式化代码。
- `npm run format:check`: 检查代码格式是否符合 Prettier 规范。
- `npm run preview`: 预览生产构建版本。
- `npm run ci`: 执行 CI 检查（构建 + lint + 格式检查）。
- `npx tsc --noEmit`: 检查 TypeScript 类型是否正确。
