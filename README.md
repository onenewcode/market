# Market - Solana ZK Identity Protocol

Market 是一个基于 Solana 区块链的去中心化身份（Identity）与信用评分（Credit Score）协议。该项目结合了 Anchor 智能合约与 React 前端，利用零知识证明（ZK Proofs）和科学的评分算法来管理链上身份与信誉。

## ✨ 主要功能 (Features)

*   **身份管理 (Identity Management)**: 用户可以在链上注册和管理自己的身份账户。
*   **信用评分 (Credit Scoring)**: 基于链上行为和验证数据计算用户的信用分数。
*   **隐私保护**: 利用 ZK 技术保护用户隐私数据（规划中）。
*   **全栈交互**: 提供完整的 React 前端界面，通过生成的 SDK 与链上程序无缝交互。

## 💡 核心概念 (Core Concepts)

### 1. 为什么需要身份证明 (Identity)?
在区块链上，默认情况下地址是匿名的，系统无法识别背后的操作者。
*   **防范女巫攻击 (Sybil Resistance)**: 防止恶意用户生成大量地址进行欺诈。
*   **状态锚定**: `IdentityAccount` 为钱包地址提供了一个链上“身份证”，记录创建时间与验证状态，使业务逻辑（如信用分）可以绑定在固定身份上。

### 2. 为什么需要信誉分 (Credit Score)?
DeFi 通常需要超额抵押，缺乏传统金融的信用评估体系。引入信誉分可以：
*   **降低抵押率**: 高分用户可享受更优的借贷条件。
*   **差异化服务**: DApp 可根据分数提供分级服务。

#### 当前评分逻辑 (Scoring Logic)
目前的评分模型基于用户的 **SOL 持有量 (Lamports)** 进行计算：

| SOL 余额 | 分数 (Score) | 等级 (Level) |
| :--- | :--- | :--- |
| ≥ 10 SOL | 90 | High |
| ≥ 1 SOL | 75 | Medium |
| ≥ 0.1 SOL | 60 | Low |
| ≥ 0.01 SOL | 45 | Low |
| 其他 | 30 | Low |

> *注：当前逻辑位于合约的 `src/instructions/score.rs`，未来将扩展更多维度（如交易历史、持有时长等）。*

## 🛠 技术栈 (Tech Stack)

*   **Smart Contract**: [Solana](https://solana.com/), [Anchor Framework](https://www.anchor-lang.com/), Rust
*   **Frontend**: [React](https://react.dev/), [Vite](https://vitejs.dev/), [Tailwind CSS](https://tailwindcss.com/)
*   **SDK Generation**: [Codama](https://github.com/codama-idl/codama)
*   **State Management**: Zustand

## 📋 环境要求 (Prerequisites)

在开始之前，请确保您的开发环境已安装以下工具：

*   **Node.js** (建议 v18+)
*   **Rust & Cargo** (最新稳定版)
*   **Solana CLI** (v1.16+)
*   **Anchor AVM** (最新版)

## 🚀 快速开始 (Quick Start)

### 1. 安装依赖

```bash
# 安装前端依赖
npm install
```

### 2. 本地环境设置

启动本地 Solana 测试网（如果您还没有运行）：

```bash
solana-test-validator
```

### 3. 构建与部署

项目提供了一个便捷的设置脚本，可以一次性完成合约构建、部署（本地）和 SDK 生成：

```bash
# 构建合约并生成 SDK
npm run setup
```

或者您可以手动分步执行：

```bash
# 1. 构建 Anchor 程序
npm run anchor-build

# 2. 生成 TypeScript SDK (确保 anchor build 已成功生成 IDL)
npm run codama:js
```

### 4. 启动前端

```bash
npm run dev
```

打开浏览器访问 `http://localhost:5173` 即可看到应用界面。

## 📂 项目结构 (Project Structure)

```
.
├── anchor/                 # Solana 智能合约 (Anchor 项目)
│   ├── programs/           # 合约源码 (Rust)
│   └── tests/              # 合约测试
├── src/                    # 前端源代码
│   ├── generated/          # Codama 自动生成的 SDK (不要手动修改)
│   ├── components/         # React 组件
│   └── ...
├── codama.json             # SDK 生成器配置
├── package.json            # 项目依赖与脚本配置
└── anchor/Anchor.toml      # Anchor 配置文件
```

## 📜 常用脚本 (Scripts)

*   `npm run dev`: 启动前端开发服务器。
*   `npm run build`: 构建前端生产版本。
*   `npm run anchor-build`: 仅构建 Solana 合约。
*   `npm run codama:js`: 仅根据 IDL 生成前端 SDK。
*   `npm run setup`: 执行完整的构建和生成流程。
*   `npm run test`: 运行测试。

## 🤝 贡献 (Contributing)

欢迎提交 Issue 和 Pull Request 来改进本项目！
