/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { IDENTITY_SCORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CANCEL_TRANSFER_DISCRIMINATOR = new Uint8Array([
  50, 32, 70, 130, 142, 41, 111, 175,
]);

export function getCancelTransferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_TRANSFER_DISCRIMINATOR,
  );
}

export type CancelTransferInstruction<
  TProgram extends string = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
  TAccountTransferRequest extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTransferRequest extends string
        ? WritableAccount<TAccountTransferRequest>
        : TAccountTransferRequest,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type CancelTransferInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelTransferInstructionDataArgs = {};

export function getCancelTransferInstructionDataEncoder(): FixedSizeEncoder<CancelTransferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_TRANSFER_DISCRIMINATOR }),
  );
}

export function getCancelTransferInstructionDataDecoder(): FixedSizeDecoder<CancelTransferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelTransferInstructionDataCodec(): FixedSizeCodec<
  CancelTransferInstructionDataArgs,
  CancelTransferInstructionData
> {
  return combineCodec(
    getCancelTransferInstructionDataEncoder(),
    getCancelTransferInstructionDataDecoder(),
  );
}

export type CancelTransferInput<
  TAccountTransferRequest extends string = string,
  TAccountOwner extends string = string,
> = {
  /** 转移请求账户（将被关闭） */
  transferRequest: Address<TAccountTransferRequest>;
  /** 转移发起者 */
  owner: TransactionSigner<TAccountOwner>;
};

export function getCancelTransferInstruction<
  TAccountTransferRequest extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
>(
  input: CancelTransferInput<TAccountTransferRequest, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): CancelTransferInstruction<
  TProgramAddress,
  TAccountTransferRequest,
  TAccountOwner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_SCORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    transferRequest: { value: input.transferRequest ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.transferRequest),
      getAccountMeta(accounts.owner),
    ],
    data: getCancelTransferInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelTransferInstruction<
    TProgramAddress,
    TAccountTransferRequest,
    TAccountOwner
  >);
}

export type ParsedCancelTransferInstruction<
  TProgram extends string = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** 转移请求账户（将被关闭） */
    transferRequest: TAccountMetas[0];
    /** 转移发起者 */
    owner: TAccountMetas[1];
  };
  data: CancelTransferInstructionData;
};

export function parseCancelTransferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelTransferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { transferRequest: getNextAccount(), owner: getNextAccount() },
    data: getCancelTransferInstructionDataDecoder().decode(instruction.data),
  };
}
