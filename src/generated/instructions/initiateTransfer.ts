/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { IDENTITY_SCORE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIATE_TRANSFER_DISCRIMINATOR = new Uint8Array([
  128, 229, 77, 5, 65, 234, 228, 75,
]);

export function getInitiateTransferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIATE_TRANSFER_DISCRIMINATOR,
  );
}

export type InitiateTransferInstruction<
  TProgram extends string = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountTransferRequest extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountTransferRequest extends string
        ? WritableAccount<TAccountTransferRequest>
        : TAccountTransferRequest,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountRecipient extends string
        ? ReadonlyAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitiateTransferInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitiateTransferInstructionDataArgs = {};

export function getInitiateTransferInstructionDataEncoder(): FixedSizeEncoder<InitiateTransferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INITIATE_TRANSFER_DISCRIMINATOR }),
  );
}

export function getInitiateTransferInstructionDataDecoder(): FixedSizeDecoder<InitiateTransferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitiateTransferInstructionDataCodec(): FixedSizeCodec<
  InitiateTransferInstructionDataArgs,
  InitiateTransferInstructionData
> {
  return combineCodec(
    getInitiateTransferInstructionDataEncoder(),
    getInitiateTransferInstructionDataDecoder(),
  );
}

export type InitiateTransferAsyncInput<
  TAccountIdentity extends string = string,
  TAccountTransferRequest extends string = string,
  TAccountOwner extends string = string,
  TAccountRecipient extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** 要转移的身份账户 */
  identity?: Address<TAccountIdentity>;
  /** 转移请求账户（自动创建） */
  transferRequest?: Address<TAccountTransferRequest>;
  /** 当前身份的所有者（发起者） */
  owner: TransactionSigner<TAccountOwner>;
  /** 接收者地址（只需传入地址，无需签名） */
  recipient: Address<TAccountRecipient>;
  /** 系统程序 */
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitiateTransferInstructionAsync<
  TAccountIdentity extends string,
  TAccountTransferRequest extends string,
  TAccountOwner extends string,
  TAccountRecipient extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
>(
  input: InitiateTransferAsyncInput<
    TAccountIdentity,
    TAccountTransferRequest,
    TAccountOwner,
    TAccountRecipient,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitiateTransferInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountTransferRequest,
    TAccountOwner,
    TAccountRecipient,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_SCORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    identity: { value: input.identity ?? null, isWritable: true },
    transferRequest: { value: input.transferRequest ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.identity.value) {
    accounts.identity.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([105, 100, 101, 110, 116, 105, 116, 121]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }
  if (!accounts.transferRequest.value) {
    accounts.transferRequest.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            116, 114, 97, 110, 115, 102, 101, 114, 95, 114, 101, 113, 117, 101,
            115, 116,
          ]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
        getAddressEncoder().encode(expectAddress(accounts.recipient.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.identity),
      getAccountMeta(accounts.transferRequest),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitiateTransferInstructionDataEncoder().encode({}),
    programAddress,
  } as InitiateTransferInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountTransferRequest,
    TAccountOwner,
    TAccountRecipient,
    TAccountSystemProgram
  >);
}

export type InitiateTransferInput<
  TAccountIdentity extends string = string,
  TAccountTransferRequest extends string = string,
  TAccountOwner extends string = string,
  TAccountRecipient extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** 要转移的身份账户 */
  identity: Address<TAccountIdentity>;
  /** 转移请求账户（自动创建） */
  transferRequest: Address<TAccountTransferRequest>;
  /** 当前身份的所有者（发起者） */
  owner: TransactionSigner<TAccountOwner>;
  /** 接收者地址（只需传入地址，无需签名） */
  recipient: Address<TAccountRecipient>;
  /** 系统程序 */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitiateTransferInstruction<
  TAccountIdentity extends string,
  TAccountTransferRequest extends string,
  TAccountOwner extends string,
  TAccountRecipient extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
>(
  input: InitiateTransferInput<
    TAccountIdentity,
    TAccountTransferRequest,
    TAccountOwner,
    TAccountRecipient,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitiateTransferInstruction<
  TProgramAddress,
  TAccountIdentity,
  TAccountTransferRequest,
  TAccountOwner,
  TAccountRecipient,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_SCORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    identity: { value: input.identity ?? null, isWritable: true },
    transferRequest: { value: input.transferRequest ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.identity),
      getAccountMeta(accounts.transferRequest),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitiateTransferInstructionDataEncoder().encode({}),
    programAddress,
  } as InitiateTransferInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountTransferRequest,
    TAccountOwner,
    TAccountRecipient,
    TAccountSystemProgram
  >);
}

export type ParsedInitiateTransferInstruction<
  TProgram extends string = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** 要转移的身份账户 */
    identity: TAccountMetas[0];
    /** 转移请求账户（自动创建） */
    transferRequest: TAccountMetas[1];
    /** 当前身份的所有者（发起者） */
    owner: TAccountMetas[2];
    /** 接收者地址（只需传入地址，无需签名） */
    recipient: TAccountMetas[3];
    /** 系统程序 */
    systemProgram: TAccountMetas[4];
  };
  data: InitiateTransferInstructionData;
};

export function parseInitiateTransferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitiateTransferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      identity: getNextAccount(),
      transferRequest: getNextAccount(),
      owner: getNextAccount(),
      recipient: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitiateTransferInstructionDataDecoder().decode(instruction.data),
  };
}
