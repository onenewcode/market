/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { IDENTITY_SCORE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const MIGRATE_IDENTITY_DISCRIMINATOR = new Uint8Array([
  161, 192, 70, 80, 47, 37, 26, 10,
]);

export function getMigrateIdentityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MIGRATE_IDENTITY_DISCRIMINATOR,
  );
}

export type MigrateIdentityInstruction<
  TProgram extends string = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountNewOwner extends string | AccountMeta<string> = string,
  TAccountOldIdentity extends string | AccountMeta<string> = string,
  TAccountNewIdentity extends string | AccountMeta<string> = string,
  TAccountOldScore extends string | AccountMeta<string> = string,
  TAccountNewScore extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountNewOwner extends string
        ? ReadonlySignerAccount<TAccountNewOwner> &
            AccountSignerMeta<TAccountNewOwner>
        : TAccountNewOwner,
      TAccountOldIdentity extends string
        ? WritableAccount<TAccountOldIdentity>
        : TAccountOldIdentity,
      TAccountNewIdentity extends string
        ? WritableAccount<TAccountNewIdentity>
        : TAccountNewIdentity,
      TAccountOldScore extends string
        ? WritableAccount<TAccountOldScore>
        : TAccountOldScore,
      TAccountNewScore extends string
        ? WritableAccount<TAccountNewScore>
        : TAccountNewScore,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MigrateIdentityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type MigrateIdentityInstructionDataArgs = {};

export function getMigrateIdentityInstructionDataEncoder(): FixedSizeEncoder<MigrateIdentityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: MIGRATE_IDENTITY_DISCRIMINATOR }),
  );
}

export function getMigrateIdentityInstructionDataDecoder(): FixedSizeDecoder<MigrateIdentityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getMigrateIdentityInstructionDataCodec(): FixedSizeCodec<
  MigrateIdentityInstructionDataArgs,
  MigrateIdentityInstructionData
> {
  return combineCodec(
    getMigrateIdentityInstructionDataEncoder(),
    getMigrateIdentityInstructionDataDecoder(),
  );
}

export type MigrateIdentityAsyncInput<
  TAccountOwner extends string = string,
  TAccountNewOwner extends string = string,
  TAccountOldIdentity extends string = string,
  TAccountNewIdentity extends string = string,
  TAccountOldScore extends string = string,
  TAccountNewScore extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  newOwner: TransactionSigner<TAccountNewOwner>;
  oldIdentity?: Address<TAccountOldIdentity>;
  newIdentity?: Address<TAccountNewIdentity>;
  oldScore?: Address<TAccountOldScore>;
  newScore?: Address<TAccountNewScore>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getMigrateIdentityInstructionAsync<
  TAccountOwner extends string,
  TAccountNewOwner extends string,
  TAccountOldIdentity extends string,
  TAccountNewIdentity extends string,
  TAccountOldScore extends string,
  TAccountNewScore extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
>(
  input: MigrateIdentityAsyncInput<
    TAccountOwner,
    TAccountNewOwner,
    TAccountOldIdentity,
    TAccountNewIdentity,
    TAccountOldScore,
    TAccountNewScore,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MigrateIdentityInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountNewOwner,
    TAccountOldIdentity,
    TAccountNewIdentity,
    TAccountOldScore,
    TAccountNewScore,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_SCORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    newOwner: { value: input.newOwner ?? null, isWritable: false },
    oldIdentity: { value: input.oldIdentity ?? null, isWritable: true },
    newIdentity: { value: input.newIdentity ?? null, isWritable: true },
    oldScore: { value: input.oldScore ?? null, isWritable: true },
    newScore: { value: input.newScore ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.oldIdentity.value) {
    accounts.oldIdentity.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([105, 100, 101, 110, 116, 105, 116, 121]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }
  if (!accounts.newIdentity.value) {
    accounts.newIdentity.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([105, 100, 101, 110, 116, 105, 116, 121]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.newOwner.value)),
      ],
    });
  }
  if (!accounts.oldScore.value) {
    accounts.oldScore.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([115, 99, 111, 114, 101])),
        getAddressEncoder().encode(expectAddress(accounts.oldIdentity.value)),
      ],
    });
  }
  if (!accounts.newScore.value) {
    accounts.newScore.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([115, 99, 111, 114, 101])),
        getAddressEncoder().encode(expectAddress(accounts.newIdentity.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.newOwner),
      getAccountMeta(accounts.oldIdentity),
      getAccountMeta(accounts.newIdentity),
      getAccountMeta(accounts.oldScore),
      getAccountMeta(accounts.newScore),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getMigrateIdentityInstructionDataEncoder().encode({}),
    programAddress,
  } as MigrateIdentityInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountNewOwner,
    TAccountOldIdentity,
    TAccountNewIdentity,
    TAccountOldScore,
    TAccountNewScore,
    TAccountSystemProgram
  >);
}

export type MigrateIdentityInput<
  TAccountOwner extends string = string,
  TAccountNewOwner extends string = string,
  TAccountOldIdentity extends string = string,
  TAccountNewIdentity extends string = string,
  TAccountOldScore extends string = string,
  TAccountNewScore extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  newOwner: TransactionSigner<TAccountNewOwner>;
  oldIdentity: Address<TAccountOldIdentity>;
  newIdentity: Address<TAccountNewIdentity>;
  oldScore: Address<TAccountOldScore>;
  newScore: Address<TAccountNewScore>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getMigrateIdentityInstruction<
  TAccountOwner extends string,
  TAccountNewOwner extends string,
  TAccountOldIdentity extends string,
  TAccountNewIdentity extends string,
  TAccountOldScore extends string,
  TAccountNewScore extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
>(
  input: MigrateIdentityInput<
    TAccountOwner,
    TAccountNewOwner,
    TAccountOldIdentity,
    TAccountNewIdentity,
    TAccountOldScore,
    TAccountNewScore,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MigrateIdentityInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountNewOwner,
  TAccountOldIdentity,
  TAccountNewIdentity,
  TAccountOldScore,
  TAccountNewScore,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_SCORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    newOwner: { value: input.newOwner ?? null, isWritable: false },
    oldIdentity: { value: input.oldIdentity ?? null, isWritable: true },
    newIdentity: { value: input.newIdentity ?? null, isWritable: true },
    oldScore: { value: input.oldScore ?? null, isWritable: true },
    newScore: { value: input.newScore ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.newOwner),
      getAccountMeta(accounts.oldIdentity),
      getAccountMeta(accounts.newIdentity),
      getAccountMeta(accounts.oldScore),
      getAccountMeta(accounts.newScore),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getMigrateIdentityInstructionDataEncoder().encode({}),
    programAddress,
  } as MigrateIdentityInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountNewOwner,
    TAccountOldIdentity,
    TAccountNewIdentity,
    TAccountOldScore,
    TAccountNewScore,
    TAccountSystemProgram
  >);
}

export type ParsedMigrateIdentityInstruction<
  TProgram extends string = typeof IDENTITY_SCORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    newOwner: TAccountMetas[1];
    oldIdentity: TAccountMetas[2];
    newIdentity: TAccountMetas[3];
    oldScore: TAccountMetas[4];
    newScore: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: MigrateIdentityInstructionData;
};

export function parseMigrateIdentityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMigrateIdentityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      newOwner: getNextAccount(),
      oldIdentity: getNextAccount(),
      newIdentity: getNextAccount(),
      oldScore: getNextAccount(),
      newScore: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMigrateIdentityInstructionDataDecoder().decode(instruction.data),
  };
}
